{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AtiendeA list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idatiende</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('atiendea_show', { 'id': entity.idatiende }) }}">{{ entity.idatiende }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('atiendea_show', { 'id': entity.idatiende }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('atiendea_edit', { 'id': entity.idatiende }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('atiendea_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
